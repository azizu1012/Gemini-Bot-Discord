================================================================================
AZURIS - PROJECT TECHNICAL INFO
================================================================================

Mục tiêu tài liệu:
- Tóm tắt kiến trúc và kỹ thuật theo trạng thái code hiện tại.
- Làm tài liệu tham chiếu nhanh cho maintainers hoặc agent phân tích ngoài.


================================================================================
1) PRODUCT GOALS
================================================================================

Azuris được thiết kế để:
- Trả lời hội thoại Discord theo ngữ cảnh.
- Dùng tool-calling để lấy thông tin thời gian thực.
- Duy trì memory dài hạn hữu dụng, không chỉ short-term.
- Bảo vệ ranh giới riêng tư giữa users trong môi trường multi-user.


================================================================================
2) RUNTIME OVERVIEW
================================================================================

Entry points:
- main.py (thin entrypoint)
- run_bot.py (launcher chính)

Luồng xử lý chính nằm ở:
- src/handlers/message_handler.py

Flow tóm tắt:
1. Nhận message (DM hoặc mention).
2. Build context gần nhất từ DB.
3. Chạy reasoning loop (có thể gọi tools).
4. Chạy final synthesis.
5. Ghi lịch sử user/assistant vào DB.


================================================================================
3) MEMORY ARCHITECTURE (DB-FIRST, HYBRID SCOPE)
================================================================================

Thành phần chính:
- src/database/repository.py
- src/managers/note_manager.py
- src/tools/tools.py (save_note/retrieve_notes bridge)

Mô hình scope:
- user: mặc định, an toàn riêng tư.
- candidate_global: fact dùng chung ở trạng thái chờ xác nhận.
- global: fact đã đủ điều kiện chia sẻ.

Policy:
- Ưu tiên retrieval theo user trước.
- Chỉ include global khi intent phù hợp.
- Chặn note mang dấu hiệu lạm dụng như impersonation/harassment/dox-like content.

Auto-promotion:
- Facts được normalize + hash.
- Khi nhiều user khác nhau xác nhận cùng fact hash, hệ thống có thể promote lên global.


================================================================================
4) DATABASE SNAPSHOT
================================================================================

Bảng chính:
- messages(user_id, role, content, timestamp)
- user_notes(user_id, note_id, content, metadata, created_at, scope, importance,
             updated_at, is_active, note_type, fact_hash)

Điểm kỹ thuật:
- Có migration idempotent để thêm cột/index an toàn.
- Có index phục vụ history retrieval và scoped note retrieval.
- Có API cho list/demote global notes.


================================================================================
5) TOOLS & SEARCH
================================================================================

Tools hiện có:
1. web_search
2. get_weather
3. calculate
4. save_note
5. retrieve_notes
6. image_recognition

Search strategy:
- Primary: DuckDuckGo streams.
- Fallback providers (khi có key và cần): SerpAPI, Tavily, Exa.


================================================================================
6) ADMIN & MODERATION FEATURES
================================================================================

Slash commands đã có:
- /global-notes
- /global-note-demote

Trải nghiệm quản trị:
- Hỗ trợ chọn qua dropdown.
- Có pagination để duyệt nhiều records dễ hơn.
- Dùng để kiểm soát tri thức shared/global và xử lý note không phù hợp.


================================================================================
7) KEY MODULE RESPONSIBILITIES
================================================================================

- src/handlers/bot_core.py:
  Discord lifecycle, slash commands, admin interaction views.

- src/handlers/message_handler.py:
  Message pipeline orchestration, model call flow, tool loop integration.

- src/tools/tools.py:
  Tool declaration and dispatch.

- src/managers/note_manager.py:
  Memory classification and retrieval policy.

- src/database/repository.py:
  SQLite persistence, schema migration, retrieval/moderation queries.

- src/services/memory_service.py:
  Legacy JSON memory utilities (không còn là source chính runtime).


================================================================================
8) WHEN TO STAY SQLITE VS MOVE POSTGRES
================================================================================

SQLite là đủ khi:
- single-node deployment,
- traffic vừa,
- ưu tiên đơn giản vận hành.

Nên cân nhắc PostgreSQL khi:
- concurrency tăng rõ,
- cần scale nhiều process/instance,
- cần observability và transaction control nâng cao.

Khuyến nghị migration:
- Giữ interface repository ổn định.
- Chuẩn bị adapter để thay backend DB dần.


================================================================================
9) NEXT PRIORITIES
================================================================================

1. Tăng test coverage cho memory policy (scope, promote/demote, blocked cases).
2. Thêm metrics theo stage (reasoning/tool/final/db latency).
3. Thêm audit trail cho admin actions trên global memory.
4. Tinh chỉnh ranking retrieval (importance + recency + relevance).

================================================================================
