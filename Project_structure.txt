Azuris_refactor_code_base/
├── README.md
├── PROJECT_INFO.txt
├── Project_structure.txt
├── AGENT_HANDOFF_PROJECT_CONTEXT.md
├── requirements.txt
├── .env.example
├── main.py
├── run_bot.py
├── run_bot.sh
├── data/
│   ├── bot_database.db (hoặc file DB theo DB_PATH)
│   ├── bot_database.db.backup (theo DB_BACKUP_PATH)
│   ├── short_term_memory.json
│   ├── weather_cache.json
│   ├── quota_state.json
│   └── voice_lock/
├── uploaded_files/
└── src/
    ├── core/
    │   ├── api_config.py
    │   ├── api_proxy.py
    │   ├── api_router.py
    │   ├── config.py
    │   ├── gemini_rate_limiter.py
    │   ├── logger.py
    │   ├── prompt_loader.py
    │   └── system_prompt.py
    ├── database/
    │   └── repository.py
    ├── handlers/
    │   ├── bot_core.py
    │   ├── bot_server.py
    │   └── message_handler.py
    ├── instructions/
    │   ├── azuris_system_prompt.txt (legacy filename, content is Chad Gibiti identity)
    │   ├── fallback_system_prompt.txt
    │   └── lite_reasoning_prompt.txt
    ├── managers/
    │   ├── cache_manager.py
    │   ├── cleanup_manager.py
    │   ├── note_manager.py
    │   └── premium_manager.py
    ├── services/
    │   ├── file_parser.py
    │   └── memory_service.py
    ├── tools/
    │   └── tools.py
    └── voice/
        └── voice_lock.py

================================================================================
ROLE MAP
================================================================================

Root files:
- main.py:
  Thin entrypoint gọi launcher chính.

- run_bot.py:
  Runtime launcher chính (bot-only hoặc bot + Flask server), log thông tin startup quan trọng.

- run_bot.sh:
  Launcher Linux/Ubuntu, hỗ trợ PM2 mode (`--pm2`) và PM2 fresh reinstall (`--pm2-fresh`).

- README.md:
  Quick start + kiến trúc ngắn gọn + command/tool overview.

- PROJECT_INFO.txt:
  Technical overview chi tiết hơn, gồm memory architecture và roadmap.

- AGENT_HANDOFF_PROJECT_CONTEXT.md:
  Context gói gọn để gửi cho agent phân tích ngoài khi không thể gửi full source.

Core runtime modules:
- src/handlers/bot_core.py:
  Khởi tạo bot, đăng ký slash commands, quản lý admin interaction views.

- src/handlers/message_handler.py:
  Pipeline xử lý message, gọi model, gọi tools, ghép final response.

Memory and persistence:
- src/database/repository.py:
  SQLite schema bootstrap/rebuild compatibility path + queries + global note moderation operations.

- src/managers/note_manager.py:
  Classification policy cho notes, retrieval scope logic, promote candidate->global.

- src/services/memory_service.py:
  Legacy JSON memory utility path.

Tool layer:
- src/tools/tools.py:
  Khai báo tools, dispatch tool-calls, và lớp ổn định web_search (cache/dedup/cooldown/deep-read).

Prompt and config:
- src/core/system_prompt.py:
  Hành vi assistant, quy tắc memory, output policy.
  (Identity đã chuyển sang Chad Gibiti)

- src/core/prompt_loader.py:
  Nạp prompt templates dùng cho các tầng model.

================================================================================
NOTES
================================================================================

- Không sửa code trong src chỉ để thay docs, trừ khi chủ động làm feature/fix.
- Khi cập nhật tính năng mới, đồng bộ lại README + PROJECT_INFO + handoff doc.
